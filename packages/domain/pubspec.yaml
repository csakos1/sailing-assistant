name: domain
description: Pure Dart domain layer — entities, value objects, use cases.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  collection: ^1.19.0
  equatable: ^2.0.5
  meta: ^1.16.0
  shared:
    path: ../shared

dev_dependencies:
  test: ^1.25.0
