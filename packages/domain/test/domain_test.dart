import 'package:test/test.dart';

void main() {
  group('domain package', () {
    test('smoke test', () {
      // Placeholder. Phase 1-ben ezt felváltják a valódi unit tesztek.
      expect(2 + 2, equals(4));
    });
  });
}
