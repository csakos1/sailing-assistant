name: data
description: Concrete implementations — NMEA TCP client, Drift DB, geomag, settings.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: ../domain
  shared:
    path: ../shared

dev_dependencies:
  flutter_test:
    sdk: flutter
