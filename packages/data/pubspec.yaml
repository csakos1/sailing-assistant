name: data
description: Concrete implementations — NMEA TCP client, Drift DB, geomag, settings.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

resolution: workspace

dependencies:
  domain:
    path: ../domain
  drift: ^2.33.0
  drift_flutter: ^0.3.0
  flutter:
    sdk: flutter
  meta: ^1.16.0
  path_provider: ^2.1.5
  shared:
    path: ../shared

dev_dependencies:
  build_runner: ^2.15.0
  drift_dev: ^2.33.0
  flutter_test:
    sdk: flutter
