name: phone
description: Sailing Assistant phone app (race-time UI for the Pixel).
publish_to: none
version: 0.1.0+1

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

resolution: workspace

dependencies:
  cupertino_icons: ^1.0.8
  data:
    path: ../../packages/data
  domain:
    path: ../../packages/domain
  flutter:
    sdk: flutter
  shared:
    path: ../../packages/shared

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
